import { Version } from '../types';
import { OSType } from './platform';
export interface BrowserInfo {
    userAgent: string;
    platform: OSType;
    platformVersion: Version | null;
}
export declare function computeBrowserInfo(userAgent: string): BrowserInfo;
export declare function parseiOSVersion(userAgent: string): Version | null;
